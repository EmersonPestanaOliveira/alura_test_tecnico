import 'package:technical_project/core/imports_core.dart';

class AppColors {
  // Cores Primárias
  static const Color primary = Color(0xFF780BF7); 
  static const Color primaryVariant = Color(0xFF430091); 

  // Cores Secundárias
  static const Color secondary = Color(0xFF8FFF24);
  static const Color secondaryVariant = Color(0xFFFF55DF); 

  // Cores de Fundo e Superfície
  static const Color background = Color(0xFFFFFFFF); 
  static const Color surface = Color(0xFFEFEFEF); 

  // Cores de Texto
  static const Color textPrimary = Color(0xFF090129); 
  static const Color textSecondary = Color(0xFF444444); 
  static const Color textHint = Color(0xFF9A9A9A); 

  // Cor de Erro
  static const Color error = Color(0xFFFF0000);

  // Cor de Destaque
  static const Color accent = Color(0xFF080127);
  static const Color accentSecundary = Color(0xFF6A82FB);

}
